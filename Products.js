import React from 'react';
import { ImageBackground, Image, Platform, StyleSheet, Text, TouchableOpacity, View, Button } from 'react-native';

export default () => (
    <View>
        <Text>This is the products page</Text>
        <Button title="change page" onPress={() => 5} />
    </View>
);